import { css } from '@emotion/core'

export const GlobalStyles = css`
    body {
        margin: 0;
        padding: 0;
    }

    * {
        box-sizing: border-box;
        outline: 0;
        font-family: Arial;
    }

    a,
    button {
        cursor: pointer;
    }
`